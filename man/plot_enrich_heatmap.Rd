% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_enrich_heatmap.R
\name{plot_enrich_heatmap}
\alias{plot_enrich_heatmap}
\title{Create a bubble heatmap of molecular signatures from CAMERA-PR or
PTM-SEA results}
\usage{
plot_enrich_heatmap(
  x,
  set_ids = NULL,
  selected_tissues = c("adipose", "blood", "muscle"),
  selected_ome = c("transcript-rna-seq", "prot-pr", "prot-ph", "metab", "prot-ol"),
  contrast_type = c("exercise_with_controls", "exercise_no_controls", "Endur_vs_Resist",
    "baseline", "control_only"),
  padj_cutoff = 0.05,
  n_top = 6L,
  zscore_colors = c("#3366ff", "darkred"),
  filename
)
}
\arguments{
\item{x}{a \code{data.frame} of CAMERA-PR or PTM-SEA results.}

\item{set_ids}{character or \code{NULL}; one or more of \code{x$set_id}. The
dataset \code{x} will be filtered to these sets. If \code{NULL}, the top 6
most significant sets from each contrast will be selected.}

\item{selected_tissues}{character; one or more tissues that will be used to
create the heatmap. If multiple tissues are selected, only those terms
tested in at least two tissues will be plotted.}

\item{selected_ome}{character; the ome that will be used to create the
heatmap.}

\item{contrast_type}{character; the type of contrasts to plot. One of
"exercise_with_controls" (default), "exercise_no_controls",
"Endur_vs_Resist", "baseline", or "control_only".}

\item{padj_cutoff}{numeric; the p-value cutoff to add an asterisk indicating statistical significance.}

\item{n_top}{integer; if \code{set_ids} is \code{NULL}, the \code{n_top} most
significant molecular signatures from each tissue and contrast will be
selected.}

\item{zscore_colors}{character; length 2 vector of colors for the smallest
and largest z-scores. Default is "#3366ff" (blue) and "darkred".}

\item{filename}{character; path to a PDF file to save the heatmap.}
}
\value{
Nothing. The heatmap is saved to a PDF file.
}
\description{
Create a heatmap of molecular signatures for a given tissue,
ome, and contrast type combination. Only works with CAMERA-PR or PTM-SEA
results.
}
\author{
Tyler Sagendorf
}
