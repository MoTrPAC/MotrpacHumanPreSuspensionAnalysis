% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pre_cawg_get_peak_annotations_hs.R
\name{pre_cawg_get_peak_annotations_hs}
\alias{pre_cawg_get_peak_annotations_hs}
\title{Annotate genomic peaks with gene-level features for human ATAC-seq or methylation data}
\usage{
pre_cawg_get_peak_annotations_hs(
  counts_dt,
  species = "Homo Sapiens",
  release = 105,
  txdb = NULL
)
}
\arguments{
\item{counts_dt}{data.table or data.frame of genomic peaks. Four input formats are
accepted: (1) data.frame whose rownames encode peaks as \code{"chrN:start-end"};
(2) data.table with \code{chrom}, \code{start}, and \code{end} columns but no
\code{feature_id} column (one is synthesized); (3) data.table that already contains
\code{chrom}, \code{start}, \code{end}, and \code{feature_id}; (4) any other
data.frame coercible to data.table with those four columns.}

\item{species}{Organism name passed to \code{txdbmaker::makeTxDbFromEnsembl}.
Default \code{"Homo Sapiens"}.}

\item{release}{Ensembl release number used to build the TxDb. Default \code{105},
matching the package-wide annotation freeze (GENCODE 39 / Ensembl v105).}

\item{txdb}{Optional pre-built \code{TxDb} object. When \code{NULL} (default) a TxDb
is constructed on the fly via \code{txdbmaker::makeTxDbFromEnsembl}. Pass a
pre-built object to avoid repeated Ensembl downloads, e.g. when calling this
function in a loop over chromosomes.}
}
\value{
A data.table with one row per input peak containing:
\describe{
\item{feature_id}{Peak identifier in \code{"chrN:start-end"} format.}
\item{chrom}{Chromosome name (chr prefix stripped).}
\item{start}{Peak start coordinate.}
\item{end}{Peak end coordinate.}
\item{width}{Peak width in bp.}
\item{chipseeker_annotation}{Verbatim annotation string from
\code{ChIPseeker::annotatePeak} (TSS region \code{c(-2000, 1000)}).}
\item{custom_annotation}{Simplified category; one of
\code{"Promoter (<=1kb)"}, \code{"Promoter (1-2kb)"},
\code{"Exon"}, \code{"Intron"}, \code{"Overlaps Gene"},
\code{"Upstream (<5kb)"}, \code{"Downstream (<5kb)"},
or \code{"Distal Intergenic"}.}
\item{ensembl_gene}{Ensembl gene ID of the nearest gene.}
\item{geneStart}{Start coordinate of the nearest gene.}
\item{geneEnd}{End coordinate of the nearest gene.}
\item{geneLength}{Length of the nearest gene in bp.}
\item{geneStrand}{Strand of the nearest gene (1 = forward, 2 = reverse).}
\item{transcriptId}{Ensembl transcript ID from the TxDb annotation.}
\item{relationship_to_gene}{Signed distance (bp) between the peak and the
nearest gene body: \code{0} when the peak overlaps the gene, negative
when upstream, positive when downstream.}
}
}
\description{
Converts a table of genomic peaks (chrom/start/end) into a fully annotated data.table
by querying an Ensembl TxDb and running ChIPseeker peak annotation. Produces a
simplified \code{custom_annotation} column that collapses ChIPseeker's verbose labels
into biologically meaningful categories, and computes each peak's signed distance to
its nearest gene body. Used in \code{data-raw/} to build the feature-to-gene mapping
stored in \code{HUMAN_FEATURE_TO_GENE} for ATAC-seq peaks (all chromosomes at once)
and MethylCap peaks (invoked per chromosome to stay within memory limits).
}
