% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\docType{data}
\name{assay_codes}
\alias{assay_codes}
\title{Assay code lookup table (vendored copy of MotrpacBicQC::assay_codes)}
\format{
A \code{data.frame} with 44 rows and 11 columns:
\describe{
\item{\code{omics_code}}{character; molecular modality code (e.g.
\code{"transcriptomics"}, \code{"epigenomics"}).}

\item{\code{submission_code}}{character; code used at data submission
(e.g. \code{"rna-seq"}, \code{"methylcap-seq"}).}

\item{\code{assay}}{character; assay identifier (e.g.
\code{"transcript-rna-seq"}).}

\item{\code{assay_code}}{character; assay code used for joins against the
\code{assay} column of differential analysis and summary stat tables.}

\item{\code{assay_name}}{character; assay name as reported by the assay
site (e.g. \code{"RRBS"}, \code{"Methyl Capture"}).}

\item{\code{cas_code}}{character; chemical analysis site responsible for
the assay (e.g. \code{"stanford"}, \code{"broad_prot"}).}

\item{\code{assay_abbreviation}}{character; short uppercase abbreviation
(e.g. \code{"TRNSCRPT"}, \code{"ATAC"}).}

\item{\code{assay_hex_colour}}{character; hexadecimal color code used to
encode the assay in figures.}

\item{\code{assay_short_text}}{character; short display label (e.g.
\code{"RNA-seq"}, \code{"RRBS"}).}

\item{\code{omics_text}}{character; display label for the molecular
modality (e.g. \code{"Transcriptomics"}).}

\item{\code{ome_text}}{character; display label for the ome (e.g.
\code{"Transcriptome"}, \code{"Epigenome"}).}
}
}
\source{
\code{MotrpacBicQC::assay_codes}, via \code{data-raw/assay_codes.R}.
}
\usage{
assay_codes
}
\description{
Lookup table mapping MoTrPAC assay codes to their human-readable
names, abbreviations, and display colors. It is used to convert internal
assay identifiers into presentation-ready labels in figures.
}
\details{
This object is a verbatim copy of \code{MotrpacBicQC::assay_codes}, taken as
a snapshot by \code{data-raw/assay_codes.R}. It is vendored here as a
temporary measure, and \code{\link{plot_single_feature}} now reads this copy
rather than the upstream object.

The motivation for vendoring is that MotrpacBicQC imports \code{inspectdf},
which has been archived on CRAN. That made MotrpacBicQC -- and therefore this
package -- uninstallable from a current CRAN snapshot without pulling
\code{inspectdf} from the CRAN Archive. Vendoring this table removed the last
use of MotrpacBicQC on an exported code path, allowing it to be dropped from
\code{Imports}.

Because this is a snapshot rather than a live reference, it can drift from
upstream: any assay added or relabelled in MotrpacBicQC will not appear here
until the copy is refreshed. Re-run \code{data-raw/assay_codes.R} to update
it. This vendoring is intended to be reverted in favor of depending on
MotrpacBicQC directly once the upstream \code{inspectdf} dependency is
resolved.
}
\seealso{
\code{\link{plot_single_feature}}, \code{\link{OME_TISSUE_CODE}}
}
\keyword{datasets}
