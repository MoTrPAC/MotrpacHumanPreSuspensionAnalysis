% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/PTMSEA_INPUT.R
\docType{data}
\name{PTMSEA_INPUT}
\alias{PTMSEA_INPUT}
\title{PTM-SEA Input Matrices}
\format{
A named list of two \code{cmapR} \code{GCT} objects, \code{"muscle"} (16,469 sites x
6 contrasts) and \code{"adipose"} (7,287 sites x 2 contrasts). The \code{mat} slot
holds the z-statistics. The \code{rdesc} slot holds the prot-ph feature
annotation and the per-contrast \code{logFC}, \code{p_value} and \code{adj_p_value}.
}
\source{
\url{https://motrpac-data.org/}
}
\usage{
PTMSEA_INPUT
}
\description{
Phosphoproteomics differential-analysis results formatted as the
input to PTM-SEA: one row per confidently localized phosphosite, one column
per acute-exercise contrast, values are the differential-analysis
z-statistics. This is an input file, not an enrichment result. PTM-SEA
itself is run from the Broad Institute PTM-SEA / ssGSEA2.0 container
against PTMsigDB, which is outside this package.

Sites are restricted to those flagged \code{confident_site} in the prot-ph
feature metadata (see \code{ADIPOSE_PROT_PH_QC}, \code{MUSCLE_PROT_PH_QC}). Contrasts
are \code{contrast_type == "exercise_with_controls"} with \code{contrast_category} in
\code{"EE-CON"} or \code{"RE-CON"}. Muscle carries three post-exercise timepoints
crossed with the two exercise modalities; adipose was sampled at
\code{post_3.5_4_hr} only.

Each element is a \code{GCT} object, the S4 class defined by the \code{cmapR}
package. This package does not depend on \code{cmapR}: the object loads without
it, but the \code{cmapR} accessors and its \code{write_gct()} need \code{cmapR} attached.
The slots are reachable as \verb{@mat}, \verb{@rdesc}, \verb{@cdesc}, \verb{@rid} and \verb{@cid}
either way.
}
\keyword{datasets,}
\keyword{proteomics}
